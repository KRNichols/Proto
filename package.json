{
  "name": "proto",
  "version": "1.0.0",
  "description": "Proprietary binary protocol format parser. To run perform a NPM INSTALL and run NPM START.",
  "main": "index.js",
  "module": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "license": "ISC",
  "dependencies": {
    "bigint-buffer": "^1.1.2",
    "esm": "^3.2.25"
  }
}
